import { View, Text } from 'react-native'
import React from 'react'

const Space = ({ Size }) => {
    return (
        <View
            style={{ height: Size }}
        >

        </View>
    )
}

export default Space